data "aws_ami" "ec2_ami" {
  most_recent = var.ami_most_recent
  owners      = [var.ami_owner]

  filter {
    name   = "name"
    values = [var.ami_name]
  }

  filter {
    name   = "virtualization-type"
    values = ["hvm"]
  }
}